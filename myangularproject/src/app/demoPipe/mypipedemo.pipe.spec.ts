import { MypipedemoPipe } from './mypipedemo.pipe';

describe('MypipedemoPipe', () => {
  it('create an instance', () => {
    const pipe = new MypipedemoPipe();
    expect(pipe).toBeTruthy();
  });
});
